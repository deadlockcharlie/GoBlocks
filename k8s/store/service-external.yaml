apiVersion: v1
kind: Service
metadata:
  name: blockstore
  namespace: goblocks
spec: 
  type: LoadBalancer
  selector: 
    app: blockstore
  ports:
  - port: 8080
    targetPort: 6000
    nodePort: 30080
    name: http